#다음과 같이 lb(파운드)단위로 입력된 몸무게를 kg(킬로그램)단위로 바꾸는 프로그램을 작성하라.
#모든 실수(소수)는 소수점 두 번째 자리에서 반올림하여 소수점  한자리까지 출력한다.
#(1lb = 0.45kg이다. 또한 lb?는 입력 전 물어보는 문장이다.)

a=float(input('lb? '))
LB=a*0.45
print(f'{a:.1f}lb is {LB:.1f}kg.')
